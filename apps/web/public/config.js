// Runtime configuration. In production the web container overwrites this file
// at startup from VITE_API_URL / VITE_WS_URL (see apps/web/docker-entrypoint.sh).
// Left empty in dev/source builds so the build-time import.meta.env values win.
window.__ELYZIAN__ = {};
