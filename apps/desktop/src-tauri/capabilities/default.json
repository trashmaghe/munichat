{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capabilities for the desktop client window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
