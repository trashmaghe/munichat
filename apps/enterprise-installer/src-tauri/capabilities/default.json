{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capabilities for the enterprise installer window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
